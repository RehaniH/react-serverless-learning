import styled from "styled-components";

export const Loader = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  bottom: 0;
  top: 0;
  left: 0;
  right: 0;
  position: absolute;
`;
